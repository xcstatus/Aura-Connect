# 模块名 - 技术规格文档

> 模板版本：v1.0
> 适用阶段：架构设计 / 开发实现 / 代码审查 / 维护交接
> 维护人：待填写
> 最后更新：待填写

---

## 一、架构概览

### 1.1 模块职责

> 用一两句话描述本模块的核心职责。

### 1.2 目录结构

```
src/模块名/
├── mod.rs              # 模块入口，公共 API 导出
├── 子模块1.rs          # 功能 A
├── 子模块2.rs          # 功能 B
└── ...
```

### 1.3 核心数据结构

> 列出本模块最重要的数据结构，说明其字段含义。

---

## 二、公开 API

### 2.1 公共函数 / 方法

> 按功能分组列出所有公共 API，标注签名。

#### 初始化与管理

```rust
pub fn setup_vault(password: &SecretString) -> anyhow::Result<VaultMeta>
pub fn verify_password(password: &SecretString, meta: &VaultMeta) -> bool
```

#### 凭据操作

```rust
pub fn save_ssh_credentials(...) -> Result<Option<String>>
pub fn load_ssh_credentials(...) -> Result<Option<SshCredentialPayload>>
```

### 2.2 类型与枚举

| 类型 | 定义位置 | 说明 |
| :--- | :--- | :--- |
| `VaultMeta` | `manager.rs` | 保险箱元数据 |
| `VaultError` | `error.rs` | 错误枚举 |

---

## 三、设计决策

> 记录重要的架构决策及其理由（参考 ADR 风格）。

### 3.1 加密方案选型

- **KDF**：Argon2id
- **对称加密**：AES-256-GCM
- **理由**：...

### 3.2 数据分离策略

- **认证密钥** vs **加密密钥**：...
- **理由**：...

### 3.3 文件持久化格式

- **格式**：JSON
- **字段**：salt + encrypted_data
- **理由**：...

---

## 四、关键实现细节

### 4.1 密钥派生流程

> 用文字或流程图描述从用户密码到加密密钥的完整路径。

### 4.2 解锁状态管理

> `CredentialVault` 的 `unlocked_key` 字段如何管理，何时清空。

### 4.3 凭据存取流程

> 从保存到加载的完整数据流。

### 4.4 异步/并发注意事项

> 本模块是否有线程安全问题，tokio runtime 使用约束等。

---

## 五、安全考量

> 从安全角度分析本模块的设计。

### 5.1 威胁模型

> 本模块防御了哪些威胁，未防御哪些。

### 5.2 内存安全

> `Zeroizing` 使用情况，敏感数据的内存清理策略。

### 5.3 已知安全局限

> 当前实现中存在的安全限制或待改进点。

---

## 六、与其他模块的边界

> 明确本模块不做什么，边界在哪里。

| 边界接口 | 协作模块 | 交互方式 |
| :--- | :--- | :--- |
| | | |

---

## 七、性能特征

> 本模块的时间/空间复杂度特征，IO 模式。

### 7.1 主要操作的复杂度

| 操作 | 时间复杂度 | 说明 |
| :--- | :--- | :--- |
| 解锁 vault | O(1) | 仅内存操作 |
| 保存凭据 | O(n) | n = vault 内凭据数量 |
| 加载凭据 | O(n) | 全量解密 |

### 7.2 IO 模式

- 每次操作：全量读取 → 修改 → 全量写入
- 文件格式：pretty-printed JSON

---

## 八、测试策略

| 测试类型 | 覆盖内容 | 测试文件位置 |
| :--- | :--- | :--- |
| 单元测试 | `core.rs` 中的生命周期测试 | `src/vault/core.rs` |
| 集成测试 | | |
| 模糊测试 | | |

---

## 九、变更历史

| 日期 | 版本 | 变更内容 | 作者 |
| :--- | :--- | :--- | :--- |
| | | | |
