修改
[patch.crates-io] 指向仓库内 vendor/cosmic-text（从 crates.io 0.15.0 拷出，去掉 fonts/ 等大目录，约 696KB）。
在 vendor/cosmic-text/src/glyph_cache.rs 里：
对 !pos.is_finite() 直接返回 (0, Zero)；
所有 trunc ± 1 改为 saturating_add(1) / saturating_sub(1)。
根 Cargo.toml 里已加简短注释说明为何打补丁。cargo build 与 cargo test --lib 已通过。
若你愿意少维护一份 vendor，以后可以等 cosmic-text / iced 上游合并同类修复后再删掉 patch；



 scripts/keyboard_matrix.sh的作用



# panic 安全性问题
let dirty = std::mem::take(&mut self.styled_dirty_rows_tmp);  // 所有权转移
self.rebuild_dirty_styled_fragments(&dirty);                   // 使用转移后的 Vec
self.styled_dirty_rows_tmp = dirty;                            // 归还所有权

 建议：如果担心 panic 安全性，可以加一个 let _ = std::mem::replace(&mut self.styled_dirty_rows_tmp, Vec::new()); 的模式，确保无论如何都能恢复。不过在 rebuild 内部 panic 的概率极低。

